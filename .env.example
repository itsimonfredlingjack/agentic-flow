# Ollama Configuration
# URL to your local Ollama instance
# Default: http://localhost:11434
# For remote Ollama: http://your-server-ip:11434
OLLAMA_BASE_URL=http://localhost:11434

# Optional: Default model to use if not specified
# Using qwen2.5-coder:3b for better chat format (starcoder2 is code-only model)
OLLAMA_DEFAULT_MODEL=qwen2.5-coder:3b

# Optional: Timeout for Ollama requests in milliseconds (default: 60000)
# OLLAMA_TIMEOUT_MS=60000
